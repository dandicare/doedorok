1. Use the component in the app/src/ui/ folder first if possible.
   If the component does not exist there, create it.
2. Remove header part from the component file because it will be developed in the native app.
3. Mobile view only. Do not create desktop view.
